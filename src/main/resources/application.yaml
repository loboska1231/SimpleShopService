spring:
  application:
    name: OrderService
  datasource:
    url: jdbc:http://localhost:5432/orders
    username: psql
    password: psql
  data:
    mongodb:
      uri: mongodb://localhost:27017/products
  jpa:
    hibernate:
      ddl-auto: none
#  liquibase:
#    enabled: false

#  mail:
#    host:
#    port:
#    password:
#    username:

server:
  port: 8080
